const math = require('./math')
const x = 5
const y = 5
console.log(`Add: ${math.addition(x,y)}`)
console.log(`Subtract: ${math.subtract(x,y)}`)
console.log(`MultiplybyThree: ${math.multiplybyThree(x)}`)